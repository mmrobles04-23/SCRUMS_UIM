version: "3.8"

services:
  app:
    build: .
    restart: unless-stopped

  web:
    image: nginx:latest
    restart: unless-stopped
    ports:
      - "3005:80"
    volumes:
      - .:/var/www
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app